function add(a, b) {
    return a + b;
}
var result = add(2, 5); // Error: this argument says that the type 'string' is not assignable to parameter of type 'number'. We can use TypeScript to catch errors before we test code.
console.log(result);
